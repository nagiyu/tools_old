name: Client対応
description: client ディレクトリ配下で作業する Issue テンプレート
title: "[Client] "
labels: ["client"]
body:
  - type: markdown
    attributes:
      value: |
        ## 注意事項
        作業は必ず **client ディレクトリ配下** で行ってください。
  - type: textarea
    id: policy
    attributes:
      label: 対応方針
      description: この Issue の対応方針を記載してください
    validations:
      required: false